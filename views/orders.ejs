<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Your Orders - BookNest</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Bootstrap & Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="icon" href="https://book-nest-wgrp.onrender.com/src/logo.ico" type="image/svg+xml">
  <link rel="stylesheet" href="/css/style.css">

  <style>
    body {
      background: linear-gradient(180deg, #e0f7fa, #ffffff);
      padding-top: 70px;
      font-family: 'Poppins', sans-serif;
      color: #000;
      overflow-x: hidden;
    }
    h2 { font-weight: 700; color: #2c3e50; }

    /* ===== Cards ===== */
    .card { border: none; border-radius: 8px; overflow: hidden; transition: transform 0.2s ease, box-shadow 0.2s ease; margin-bottom: 30px; background-color: #ECEFF1; }
    .card:hover { transform: translateY(-4px); box-shadow: 0 8px 18px rgba(0, 0, 0, 0.08); }
    .card-body { background: #fff; border-left: 4px solid #007bff; padding: 20px; }

    /* ===== Progressbar ===== */
    #progressbar {
      display: flex;
      justify-content: space-between;
      margin-bottom: 20px;
      padding-left: 0;
      position: relative;
    }
    #progressbar li {
      list-style: none;
      width: 33.33%;
      text-align: center;
      font-size: 12px;
      font-weight: 500;
      color: #999;
      position: relative;
    }
    #progressbar li:before {
      content: '';
      width: 40px;
      height: 40px;
      background: #C5CAE9;
      border-radius: 50%;
      display: block;
      margin: auto;
      z-index: 1;
      position: relative;
      line-height: 40px;
      font-size: 20px;
      color: #fff;
    }
    #progressbar li:after {
      content: '';
      position: absolute;
      width: 100%;
      height: 4px;
      background: #C5CAE9;
      top: 18px;
      left: -50%;
      z-index: 0;
    }
    #progressbar li:first-child:after { left: 50%; border-radius: 2px 0 0 2px; }
    #progressbar li:last-child:after { left: -50%; border-radius: 0 2px 2px 0; }

    /* Active Steps Colors */
    #progressbar li.active-processed:before { background: #FFD700; content: '✓'; }
    #progressbar li.active-shipped:before { background: #FF8C00; content: '✓'; }
    #progressbar li.active-arrived:before { background: #28a745; content: '✓'; }

    #progressbar li.active-processed { color: #FFD700; }
    #progressbar li.active-shipped { color: #FF8C00; }
    #progressbar li.active-arrived { color: #28a745; }

    #progressbar li.active-processed + li:after { background: #FFD700; }
    #progressbar li.active-shipped + li:after { background: #FF8C00; }
    #progressbar li.active-arrived + li:after { background: #28a745; }

    /* ===== Images & Table ===== */
    .order-img { width: 60px; height: 80px; object-fit: cover; border-radius: 6px; box-shadow: 0 2px 6px rgba(0,0,0,0.1); }
    .table { border-radius: 6px; overflow: hidden; }
    .table thead { background-color: #f8f9fa; }
    .table tbody tr:hover { background-color: #f1f6ff; }

    /* ===== Badges & Buttons ===== */
    .badge { padding: 0.45em 0.7em; border-radius: 6px; font-size: 0.85rem; font-weight: 500; }
    .btn-outline-danger.btn-sm, .btn-outline-info.btn-sm { border-radius: 6px; font-size: 0.85rem; transition: all 0.3s ease; }
    .btn-outline-danger:hover { background-color: #dc3545; color: #fff; }
    .btn-outline-info:hover { background-color: #0dcaf0; color: #fff; }

    /* ===== Delivery Info ===== */
    .delivery-info { background: #f9fafc; border-radius: 6px; padding: 10px 15px; margin-top: 15px; border-left: 4px solid #17a2b8; }

    @media screen and (max-width: 992px) { #progressbar li { font-size: 10px; } }
  </style>
</head>
<body>

<%- include('partials/header') %>

<div class="container mt-3">
  <h2 class="text-center mb-4">📦 Your Orders</h2>

  <% if (orders.length > 0) { %>
    <% orders.forEach(order => { %>
      <div class="card">
        <div class="card-body">

          <!-- Progress Tracker -->
          <div class="container px-1 px-md-4 py-3">
            <div class="row d-flex justify-content-between px-3 top">
              <div class="d-flex">
                <h5>ORDER ID: <span class="text-primary font-weight-bold"><%= order._id %></span></h5>
              </div>
            </div>

            <div class="row d-flex justify-content-center">
              <div class="col-12">
                <ul id="progressbar" class="text-center">
                  <li class="<%= ['Processing','Shipped','Delivered'].indexOf(order.status) >= 0 ? 'active-processed' : '' %>"></li>
                  <li class="<%= ['Shipped','Delivered'].indexOf(order.status) >= 1 ? 'active-shipped' : '' %>"></li>
                  <li class="<%= order.status === 'Delivered' ? 'active-arrived' : '' %>"></li>
                </ul>
              </div>
            </div>

            <div class="row justify-content-between top text-center">
              <div class="col d-flex flex-column align-items-center">
                <img class="icon mb-1" src="https://i.imgur.com/9nnc9Et.png" alt="Processed" width="40">
                <p class="font-weight-bold small">Order<br>Processed</p>
              </div>
              <div class="col d-flex flex-column align-items-center">
                <img class="icon mb-1" src="https://i.imgur.com/u1AzR7w.png" alt="Shipped" width="40">
                <p class="font-weight-bold small">Order<br>Shipped</p>
              </div>
              <div class="col d-flex flex-column align-items-center">
                <img class="icon mb-1" src="https://i.imgur.com/HdsziHP.png" alt="Arrived" width="40">
                <p class="font-weight-bold small">Order<br>Arrived</p>
              </div>
            </div>
          </div>

          <!-- Order Details -->
          <div class="d-flex justify-content-between align-items-center flex-wrap mt-3">
            <span class="text-muted small"><i class="bi bi-calendar3"></i> <%= order.orderDate %></span>
          </div>

          <p class="mb-1"><strong>📍 Address:</strong> <%= order.address %></p>
          <p class="mb-3"><strong>📞 Contact:</strong> <%= order.phone %></p>

          <!-- Order Items -->
          <div class="table-responsive mt-3">
            <table class="table align-middle mb-0">
              <thead class="table-light">
                <tr>
                  <th>Cover</th>
                  <th>Book</th>
                  <th>Price (₹)</th>
                </tr>
              </thead>
              <tbody>
                <% order.items.forEach(item => { %>
                  <tr>
                    <td class="text-center"><img src="<%= item.image || '/src/books/default-book.png' %>" alt="<%= item.title %>" class="order-img"></td>
                    <td><%= item.title %></td>
                    <td><strong>₹<%= item.price.toFixed(2) %></strong></td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>

          <!-- Summary & Payment -->
          <div class="mt-3 border-top pt-2">
            <p><strong>Total:</strong> ₹<%= order.grandTotal %></p>
            <p><strong>Payment:</strong> <%= order.paymentMethod || 'Prepaid' %> 
              <% if (order.paymentStatus === 'Paid') { %>
                <span class="badge bg-success">Paid</span>
              <% } else if (order.paymentStatus === 'Pending') { %>
                <span class="badge bg-warning text-dark">Pending</span>
              <% } else if (order.paymentStatus === 'Failed') { %>
                <span class="badge bg-danger">Failed</span>
              <% } else if (order.paymentStatus === 'Refunded') { %>
                <span class="badge bg-info text-dark">Refunded</span>
              <% } %>
            </p>
          </div>

          <!-- Delivery Info -->
          <div class="delivery-info mt-3">
            <p class="mb-1"><strong>📅 Expected Delivery:</strong> <%= order.deliveryDate %></p>
            <p class="mb-1"><strong>🚚 Partner:</strong> <%= order.deliveryPartner %></p>
            <p class="mb-0"><strong>📦 Status:</strong>
              <% if (order.status === 'Delivered') { %>
                <span class="badge bg-success">Delivered</span>
              <% } else if (order.status === 'Cancelled') { %>
                <span class="badge bg-danger">Cancelled</span>
              <% } else { %>
                <span class="badge bg-warning text-dark">Processing</span>
              <% } %>
            </p>
          </div>

          <!-- Cancel Button -->
          <% if (order.status === 'Processing' && order.orderDate === today) { %>
            <div class="d-flex align-items-center gap-2 mt-3">
              <button type="button" class="btn btn-outline-danger btn-sm btn-cancel-order" 
                      data-bs-toggle="modal" data-bs-target="#cancelModal"
                      data-order-id="<%= order._id %>">
                <i class="bi bi-x-circle me-1"></i> Cancel Order
              </button>
              <button type="button" class="btn btn-outline-info btn-sm" data-bs-toggle="modal" data-bs-target="#termsModal">
                <i class="bi bi-info-circle"></i> Terms
              </button>
            </div>
          <% } %>

        </div>
      </div>
    <% }) %>
  <% } else { %>
    <div class="text-center py-5">
      <img src="/images/empty-box.svg" alt="No Orders" class="mb-3" width="120">
      <p class="text-muted">You have no orders yet. Start shopping <a href="/products" class="text-primary fw-semibold">here</a>!</p>
    </div>
  <% } %>
</div>

<!-- CANCEL MODAL -->
<div class="modal fade" id="cancelModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header bg-light">
        <h5 class="modal-title text-danger"><i class="bi bi-exclamation-triangle-fill me-2"></i>Confirm Cancellation</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body">
        Are you sure you want to cancel this order? This action cannot be undone.
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary rounded-pill px-4" data-bs-dismiss="modal">No, Keep Order</button>
        <form id="cancelForm" method="POST">
          <button type="submit" class="btn btn-danger rounded-pill px-4">Yes, Cancel Order</button>
        </form>
      </div>
    </div>
  </div>
</div>

<!-- TERMS MODAL -->
<div class="modal fade" id="termsModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header bg-light">
        <h5 class="modal-title"><i class="bi bi-info-circle me-2 text-info"></i>Order Cancellation Terms</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body">
        <ul class="mb-0">
          <li>Order cancellation is only allowed on the same day of purchase.</li>
          <li>Orders already shipped cannot be cancelled.</li>
          <li>Refunds will be processed within 5–7 business days.</li>
          <li>Refunds are credited to the original payment method.</li>
          <li>BookNest reserves the right to deny cancellation if the order is packed or dispatched.</li>
        </ul>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary rounded-pill px-4" data-bs-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script>
  // Dynamically set order ID for cancel form
  const cancelModal = document.getElementById('cancelModal');
  const cancelForm = document.getElementById('cancelForm');

  cancelModal.addEventListener('show.bs.modal', function (event) {
    const button = event.relatedTarget;
    const orderId = button.getAttribute('data-order-id');
    cancelForm.action = `/orders/cancel/${orderId}`;
  });
</script>
</body>
</html>